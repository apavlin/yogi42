mixin nav(className, items, activeIndex)
	nav(class=className)
		.container
			ul(class="#{className}__list")
				each item, index in items
					li(class="#{className}__item" + ((activeIndex == index) ? "_active" : ''))
						a(href="#" class="#{className}__link")= item